# no-unresolved-false-positive

> False positive with no-unresolved rule

```sh
npm install && npm run lint
```

Downgrade `webpack` version to make it work:

```sh
npm install webpack@4.44.2 webpack-cli@3.3.12 && npm run lint
```